# UiPath Assets

This directory will hold source-controlled UiPath artifacts as they are created:

- Maestro BPMN process definitions.
- API workflow definitions.
- Data Service schema notes or export manifests.
- Action Center task schemas.
- Agent prompt/configuration notes.
- Test Cloud project mapping.

Generated local skill bundles and credentials are ignored by git.
