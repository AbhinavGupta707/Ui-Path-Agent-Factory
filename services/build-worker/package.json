{
  "name": "@agent-factory/build-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "npm --workspace @agent-factory/shared-contracts run build",
    "build": "tsc -p tsconfig.json",
    "dev": "node dist/server.js",
    "pretest": "npm --workspace @agent-factory/shared-contracts run build",
    "smoke": "npm run build && npm test",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-factory/shared-contracts": "0.1.0"
  }
}
